# PS 47 — Core Backend dependencies
# Phase 1: Foundation (FastAPI + PostgreSQL + Auth)

# Web framework
fastapi==0.111.1
uvicorn[standard]==0.30.1

# Database
sqlalchemy==2.0.31
alembic==1.13.2
psycopg2-binary==2.9.9   # PostgreSQL driver — requires pg_config/libpq; skip for SQLite-only tests
# For tests we use SQLite (built-in), so no extra driver needed

# Pydantic (FastAPI uses v2 by default)
pydantic==2.8.2
pydantic-settings==2.3.4

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9   # required for Form / file uploads

# Utilities
python-dotenv==1.0.1

# Testing
pytest==8.3.2
pytest-asyncio==0.23.8
httpx==0.27.0             # ASGI test client for FastAPI
